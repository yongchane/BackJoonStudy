
알고리즘 설명:
1. 먼저 for 문을 통해 학생 수에 대한 배열을 만듬
2. 28개의 숫자를 입력 받기 위해 for 문을 사용하여 입력을 받고 각각의 입력한 숫자를 1번에서 실행한 배열에서 빼도록 remote를 사용
3. min,max를 통해 배열의 최소 최댓값 출력
-----------------------

students = [i for i in range(1,31)]
#  학생수를 1부터 30까지 지정

for _ in range(28):
    applied = int(input())
    students.remove(applied) #소거

# 입력 창이랑 1부터 30인 학생수에서 입력한 수 제거

#최소 최대 두줄로 뽑음
print(min(students))
print(max(students))

